// Base44 client integration is intentionally disabled in the local StudyClaw shell.
// The generated app now talks directly to the local Python backend via src/lib/api.js.
export const base44 = null;
